class Solution(object):
    def search(self, nums, target):
        """
        :type nums: List[int]
        :type target: int
        :rtype: int
        """
        # n = len(nums)
        # left = 0
        # right = n - 1
        # index = -1

        # while left <= right:
        #     mid = (left + right) // 2
        #     if nums[mid] == target:
        #         index = mid
        #         return index
        #     elif nums[mid] < target:
        #         left = mid + 1
        #     else:
        #         right = mid + 1
        # return index

        index = -1
        for i in range(0, len(nums)):
            if nums[i] == target:
                index = i
        return index
